import styled from 'styled-components'

export const OrderBookTradesStyled = styled.div`
  display: flex;
  /* width: 100%; */
  height: 100%;
  width: 31rem;
  background: var(--color-primary);
  border-right: 0.1rem solid var(--color-border);
  padding: 0;
  grid-area: orderbook;
  align-self: start;
`
