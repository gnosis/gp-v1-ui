import styled from 'styled-components'

export const StrongSubHeader = styled.strong`
  box-sizing: border-box;
  color: var(--color-text-primary);
  display: flex;
  font-size: 1.5rem;
  margin-right: 0.5rem;
  text-transform: capitalize;
`
